import type { PlayerInMatchWrapper } from "./player-in-match";

/**
 * WIP and only used by other WIP/unused implementations.
 */
export class TeamWrapper {
  constructor(public players: PlayerInMatchWrapper[]) {}
}
